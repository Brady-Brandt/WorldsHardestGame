The level .txt files specify the format of a the level


Parser takes in one block at a time 
Each block is seperated by two new lines
variables only exist within that block
Each block starts with the type of object then newline
Then any variable declared like x=5 with no spaces

functions builtin:
	arguements are passed with a comma space
	func(a, b)`


Checkpoints


Moving enemies 

Stagnant enemies

Border lines

Start location

